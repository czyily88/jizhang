# 收支金额符号和颜色规范 ✅

## 📋 规范要求

| 类型 | 数值符号 | 显示符号 | 颜色 (负数) | 颜色 (正数) | HEX 值 |
|------|---------|---------|------------|------------|-------|
| **支出** | 负数 (-) | `-XX.XX` | 绿色 | - | #2ed573 |
| **收入** | 正数 (+) | `+XX.XX` | - | 红色 | #ff4757 |
| **结余/统计** | 可正可负 | `+/-XX.XX` | 绿色 | 红色 | #2ed573/#ff4757 |

### 颜色说明
- **绿色 (#2ed573)**: 用于**负数**（支出、亏损、负结余）
- **红色 (#ff4757)**: 用于**正数**（收入、盈利、正结余）

> ⚠️ 注意：这是非传统的配色方案（通常红色代表支出/亏损），但符合用户的明确要求。

---

## ✅ 已更新的页面和组件

### 1. HomeScreen.tsx (首页)
- **列表项**: 
  - 支出：`-¥10.00` (绿色 #2ed573)
  - 收入：`+¥20.00` (红色 #ff4757)
- **统计卡片**: 
  - 支出：`-10.00` (绿色 #2ed573)
  - 收入：`+20.00` (红色 #ff4757)
  - 结余：`+/-XX.XX` (正数红色，负数绿色)

### 2. StatsView.tsx (统计页面)
- **筛选结果卡片**:
  - 支出：`-XX.XX` (绿色 #2ed573)
  - 收入：`+XX.XX` (红色 #ff4757)
  - 结余：`+/-XX.XX` (正数红色，负数绿色)
- **消费分类统计**:
  - 金额：`-XX.XX` (绿色 #2ed573)
- **收入分类统计**:
  - 金额：`+XX.XX` (红色 #ff4757)
- **付款方式统计**:
  - 根据实际数值显示 `+/-XX.XX` (正数红色，负数绿色)
- **账单明细**:
  - 支出：`-XX.XX` (绿色 #2ed573)
  - 收入：`+XX.XX` (红色 #ff4757)

### 3. AddExpenseScreen.tsx (记一笔/编辑)
- **类型切换按钮**:
  - 支出激活：背景色 #2ed573 (绿色)
  - 收入激活：背景色 #ff4757 (红色)

---

## 🔧 技术实现细节

### 样式定义位置

#### HomeScreen.tsx
```typescript
expenseAmountExpense: { color: '#2ed573' },   // 绿色 - 支出
expenseAmountIncome: { color: '#ff4757' },   // 红色 - 收入
summaryValueExpense: { color: '#2ed573' },    // 绿色 - 支出
summaryValueIncome: { color: '#ff4757' },    // 红色 - 收入
summaryValuePositive: { color: '#ff4757' },   // 红色 - 正结余
summaryValueNegative: { color: '#2ed573' },   // 绿色 - 负结余
```

#### StatsView.tsx
```typescript
statValueExpense: { color: '#2ed573' },      // 绿色 - 支出
statValueIncome: { color: '#ff4757' },      // 红色 - 收入
expenseAmountExpense: { color: '#2ed573' },  // 绿色 - 支出
expenseAmountIncome: { color: '#ff4757' },  // 红色 - 收入
positive: { color: '#ff4757' },              // 红色 - 正数
negative: { color: '#2ed573' },              // 绿色 - 负数
typeChipExpense: { backgroundColor: '#f0fff0', borderColor: '#2ed573' },
typeChipTextExpense: { color: '#2ed573' },
typeChipIncome: { backgroundColor: '#fff0f0', borderColor: '#ff4757' },
typeChipTextIncome: { color: '#ff4757' },
catAmount: { ... negative/positive styling }
```

#### AddExpenseScreen.tsx
```typescript
typeBtnActiveExpense: { backgroundColor: '#2ed573' },  // 绿色 - 支出
typeBtnActiveIncome: { backgroundColor: '#ff4757' },  // 红色 - 收入
```

---

## 📝 显示格式示例

```
首页列表:
  🍜 餐饮             -15.50  ← 绿色 + 负号
                     14:30
  
  💰 工资            +5000.00  ← 红色 + 正号
                      昨天 10:00

首页统计卡片:
  ┌─────────────────────────┐
  │ 今日收支                 │
  │ ───────┬────────┬───────│
  │ 支出    │ 收入   │ 结余   │
  │ -156.00 │ +5200.00 │ +5044.00 │  ← 红色 + 正号
  │ (绿)   │ (红)   │ (红)    │
  └─────────────────────────┘

统计页面 - 消费分类:
  🍜 餐饮          -156.00  ← 绿色 + 负号
  ━━━━━━━━━━━━━━━━ 45%
  
  🎮 娱乐          -89.00   ← 绿色 + 负号
  ━━━━━━━━━━━━━━━━ 25%

统计页面 - 收入分类:
  💰 工资          +5000.00  ← 红色 + 正号
  ━━━━━━━━━━━━━━━━ 92%
```

---

## 🎨 配色方案总结

| 用途 | 数值 | 颜色 | HEX | RGB |
|------|------|-----|-----|-----|
| 支出/负结余/负数 | < 0 | 绿色 | #2ed573 | rgb(46, 213, 115) |
| 收入/正结余/正数 | > 0 | 红色 | #ff4757 | rgb(255, 71, 87) |

---

## ✅ 验证清单

- [x] 首页列表金额带符号且颜色正确
- [x] 首页统计卡片金额带符号且颜色正确
  - [x] 支出显示为 `-XX.XX` 绿色
  - [x] 收入显示为 `+XX.XX` 红色
  - [x] 结余正数显示为 `+XX.XX` 红色
  - [x] 结余负数显示为 `-XX.XX` 绿色
- [x] 统计页面筛选结果带符号且颜色正确
- [x] 统计页面消费分类带符号且颜色正确
- [x] 统计页面收入分类带符号且颜色正确
- [x] 统计页面付款方式统计带符号且颜色正确
- [x] 统计页面账单明细带符号且颜色正确
- [x] 记一笔页面类型切换按钮颜色正确
